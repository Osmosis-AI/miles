apiVersion: v1
kind: Pod
metadata:
  name: andy-miles-dev-node
  namespace: trainers
  labels:
    app: andy-miles-dev-node
    owner: andy
    purpose: miles-feature-verification
spec:
  priorityClassName: skypilot-high-priority
  preemptionPolicy: PreemptLowerPriority
  restartPolicy: Never
  hostNetwork: true
  dnsPolicy: ClusterFirstWithHostNet
  terminationGracePeriodSeconds: 30
  nodeSelector:
    nvidia.com/gpu.product: NVIDIA-H200
  containers:
    - name: trainer
      image: docker.io/radixark/miles@sha256:c6e09b6b20cad09aefe312bc4311bf6d4978b9ff2679d41b2f80fa0e4d19d12a
      imagePullPolicy: IfNotPresent
      command: ["bash", "-lc", "sleep infinity"]
      securityContext:
        privileged: true
      env:
        - name: HF_HOME
          value: /workspace/hf_cache
        - name: PYTHONUNBUFFERED
          value: "1"
        - name: NCCL_TIMEOUT_MS
          value: "36000000"
      resources:
        requests:
          cpu: "32"
          memory: 512Gi
          nvidia.com/gpu: "8"
        limits:
          cpu: "120"
          memory: 1600Gi
          nvidia.com/gpu: "8"
      volumeMounts:
        - name: weka-data
          mountPath: /workspace
        - name: dshm
          mountPath: /dev/shm
  volumes:
    - name: weka-data
      persistentVolumeClaim:
        claimName: weka-data
    - name: dshm
      emptyDir:
        medium: Memory
        sizeLimit: 512Gi
